# Supabase (anon/publishable key only — never use service_role in the client)
# Get these from: Supabase Dashboard → Project Settings → API
VITE_SUPABASE_URL=https://jwcrrnevvtxaycsqjmem.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Vercel: Project Settings → Environment Variables
# Add the same two keys for Production and Preview (and Development if using `vercel dev`).

# Server-only (Vercel /api/sync-finca — NEVER prefix with VITE_)
# Same value as ~/.finca-db.env DATABASE_URL
FINCA_DATABASE_URL=

# Postgres FYM (fuera del repo): ~/.fuckyoumoney-db.env con DATABASE_URL
# Dashboard Supabase → Settings → Database → URI.
# Luego: node scripts/run-sql.mjs supabase/migrations/001_wealth_os.sql
# Briefing IA: node scripts/dump-wealth.mjs
