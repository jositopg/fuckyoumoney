# Supabase (anon/publishable key only — never use service_role in the client)
# Get these from: Supabase Dashboard → Project Settings → API
VITE_SUPABASE_URL=https://jwcrrnevvtxaycsqjmem.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Vercel: Project Settings → Environment Variables
# Add the same two keys for Production and Preview (and Development if using `vercel dev`).

# Server-only (Vercel /api — NEVER prefix with VITE_)
# Same value as ~/.finca-db.env DATABASE_URL
FINCA_DATABASE_URL=

# SpaceXAI / xAI (preferred). Fallback: Vercel AI Gateway OIDC.
XAI_API_KEY=

# Optional: apply supabase/migrations/001_wealth_os.sql with
# DATABASE_URL in ~/.fuckyoumoney-db.env (not this file).
